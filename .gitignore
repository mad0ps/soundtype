# Собранные пакеты и промежуточная сборка
build/
*.click

# Скачиваемое: модель и библиотеки живут в каталоге данных приложения,
# в репозиторий не попадают (около 800 МБ)
models/
vendor/
runtime/

__pycache__/
*.pyc

.venv/

ut-build/
build/

# eval harness private data (Khan's voice and texts — never commit)
eval/corpus/
eval/runs/
eval/reports/
